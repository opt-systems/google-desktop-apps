{
  "name": "google-desktop-apps",
  "version": "1.0.0",
  "description": "Google Desktop Apps",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:tasks-light": "node src/tasks/build.js",
    "build:tasks-dark": "node src/tasks/build-dark.js",
    "build:tasks": "yarn build:tasks-light && yarn build:tasks-dark",
    "build:keep-light": "node src/keep/build.js",
    "build:keep-dark": "node src/keep/build-dark.js",
    "build:keep": "yarn build:keep-light && yarn build:keep-dark",
    "build:docs-light": "node src/docs/build.js",
    "build:docs-dark": "node src/docs/build-dark.js",
    "build:docs": "yarn build:docs-light && yarn build:docs-dark",
    "build:sheets-light": "node src/sheets/build.js",
    "build:sheets-dark": "node src/sheets/build-dark.js",
    "build:sheets": "yarn build:sheets-light && yarn build:sheets-dark",
    "build:slides-light": "node src/slides/build.js",
    "build:slides-dark": "node src/slides/build-dark.js",
    "build:slides": "yarn build:slides-light && yarn build:slides-dark",
    "build:forms-light": "node src/forms/build.js",
    "build:forms-dark": "node src/forms/build-dark.js",
    "build:forms": "yarn build:forms-light && yarn build:forms-dark",
    "build:drive-light": "node src/drive/build.js",
    "build:drive-dark": "node src/drive/build-dark.js",
    "build:drive": "yarn build:drive-light && yarn build:drive-dark",
    "build:photos-light": "node src/photos/build.js",
    "build:photos-dark": "node src/photos/build-dark.js",
    "build:photos": "yarn build:photos-light && yarn build:photos-dark",
    "build:calendar-light": "node src/calendar/build.js",
    "build:calendar-dark": "node src/calendar/build-dark.js",
    "build:calendar": "yarn build:calendar-light && yarn build:calendar-dark",
    "build:gmail-light": "node src/gmail/build.js",
    "build:gmail-dark": "node src/gmail/build-dark.js",
    "build:gmail": "yarn build:gmail-light && yarn build:gmail-dark",
    "build:all": "yarn build:tasks && yarn build:keep && yarn build:docs && yarn build:sheets && yarn build:slides && yarn build:forms && yarn build:drive && yarn build:photos && yarn build:calendar && yarn build:gmail",
    "build": "yarn build:all"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/opt-systems/google-desktop-apps.git"
  },
  "author": "",
  "license": "ISC",
  "homepage": "https://opt.si/google-desktop-apps",
  "devDependencies": {
    "nativefier": "^11.0.2"
  }
}
